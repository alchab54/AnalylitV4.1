# Utilise l'image de base qui contient Python et toutes les dépendances
FROM analylit-base

# Copier le code de l'application
COPY . .

EXPOSE 5000 # Le port est défini à 5000 dans docker-compose.yml
