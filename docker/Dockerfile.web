# Utilise l'image de base
FROM analylit-base:latest

# Copier le code de l'application (déjà fait dans la base, mais on s'assure que c'est à jour)
COPY . /home/appuser/app

# Rendre l'entrypoint exécutable
RUN chmod +x /home/appuser/app/docker/entrypoint.sh

# Le point d'entrée est maintenant géré par docker-compose
