# Hérite de l'image de base (code DÉJÀ présent)
FROM analylit-base:latest

# Configuration spécifique au service web
USER root
RUN chmod +x docker/entrypoint.sh
USER appuser

# Exposition du port
EXPOSE 5000

# Point d'entrée spécifique au web
ENTRYPOINT ["docker/entrypoint.sh"]
