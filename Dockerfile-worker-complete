# AnalyLit V4.0 - Dockerfile pour les workers avec support GPU
FROM nvidia/cuda:12.3.1-runtime-ubuntu22.04

# Éviter les prompts interactifs
ENV DEBIAN_FRONTEND=noninteractive
ENV TZ=Europe/Paris

# Installation Python 3.11
RUN apt-get update && apt-get install -y \
    software-properties-common \
    tzdata \
    && add-apt-repository ppa:deadsnakes/ppa \
    && apt-get update && apt-get install -y \
    python3.11 \
    python3.11-dev \
    python3.11-venv \
    python3-pip \
    gcc \
    g++ \
    curl \
    build-essential \
    git \
    && ln -s /usr/bin/python3.11 /usr/bin/python \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app

# Mise à jour de pip pour Python 3.11
RUN python -m pip install --upgrade pip

# Installation des requirements Python
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Variables d'environnement pour GPU
ENV NVIDIA_VISIBLE_DEVICES=all
ENV NVIDIA_DRIVER_CAPABILITIES=compute,utility
ENV CUDA_VISIBLE_DEVICES=0
ENV USE_GPU=true

# Copie des fichiers de configuration
COPY config_v4.py .
COPY tasks_v4_complete.py .

# Création du répertoire des projets
RUN mkdir -p /app/projects

# Pré-téléchargement du modèle d'embedding
ENV SENTENCE_TRANSFORMERS_HOME=/app/model_cache
RUN python -c "from sentence_transformers import SentenceTransformer; model = SentenceTransformer('all-MiniLM-L6-v2'); model.save('/app/model_cache/all-MiniLM-L6-v2')"

# Variables d'environnement
ENV PYTHONPATH=/app
ENV PYTHONUNBUFFERED=1

# Commande de démarrage du worker RQ
CMD ["python", "-m", "rq", "worker", "-u", "redis://redis:6379/0", "--with-scheduler", "analylit_processing_v4", "analylit_synthesis_v4", "analylit_analysis_v4", "analylit_background_v4"]
