# ===================================================================
# == ANALYLIT V4.1 - FICHIER D'EXEMPLE DE CONFIGURATION
# ===================================================================
#
# Ce fichier sert de modèle. Copiez-le en '.env' et remplissez les
# valeurs manquantes ou secrètes. Ne committez jamais le fichier .env !

# ===============================================
# == Configuration Flask Application ==
# ===============================================
# Clé secrète pour sécuriser les sessions. Changez-la pour une valeur aléatoire.
# Vous pouvez en générer une avec : python -c 'import secrets; print(secrets.token_hex())'
SECRET_KEY=your-super-secret-and-long-random-string
FLASK_ENV=production
LOG_LEVEL=INFO

# ===============================================
# == Connexions Services Docker ==
# ===============================================
# Ces URLs utilisent les noms des services Docker (db, redis, ollama)
DATABASE_URL=postgresql+psycopg2://analylit_user:strong_password@db:5432/analylit_db
# ✅ CORRECTION: L'URL de la base de données de test doit pointer vers le service 'analylit_test_db', et non 'db'.
TEST_DATABASE_URL=postgresql+psycopg2://analylit_user:strong_password@analylit_test_db:5432/analylit_test_db
REDIS_URL=redis://redis:6379/0
OLLAMA_BASE_URL=http://ollama:11434

# ===============================================
# == MOTEUR IA ==
# ===============================================
# Modèles IA par défaut utilisés par l'application
EMBEDDING_MODEL=all-MiniLM-L6-v2
LLM_MODEL=llama3:8b
FAST_LLM_MODEL=llama3.2:3b

# ===============================================
# == Configuration PostgreSQL Container ==
# ===============================================
# Ces variables sont utilisées par le conteneur 'db' pour s'initialiser.
# Le mot de passe doit correspondre à celui dans DATABASE_URL.
POSTGRES_USER=analylit_user
POSTGRES_PASSWORD=strong_password
POSTGRES_DB=analylit_db

# ===============================================
# == Performance ==
# ===============================================
# Paramètres pour Gunicorn (serveur d'application)
GUNICORN_WORKERS=4
GUNICORN_THREADS=2
GUNICORN_TIMEOUT=120

# ===============================================
# == API Keys Externes (Optionnel) ==
# ===============================================
# Remplacer par vos propres clés si vous utilisez ces services
UNPAYWALL_EMAIL=your.email@example.com
ZOTERO_USER_ID=
ZOTERO_API_KEY=
ZOTERO_GROUP_ID=

IEEE_API_KEY=
PUBMED_API_KEY=
CROSSREF_EMAIL=your.email@example.com
