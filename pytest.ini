[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
python_classes = Test*
addopts = 
    --tb=short 
    -v
    -n 1
    --timeout=60
    --disable-warnings

timeout = 60
markers =
    slow: marque les tests lents
    integration: tests d'intégration
    unit: tests unitaires
    skip_workers: skip worker tests that require RQ infrastructure
