; pytest.ini
[pytest]
addopts = -ra -q --cov=utils --cov=server_v4_complete --cov-report=html
markers =
    integration: integration tests requiring external services
    gpu: tests requiring GPU resources
    slow: slow tests that take more than 30 seconds
testpaths = tests
python_files = test_*.py
pythonpath = . 