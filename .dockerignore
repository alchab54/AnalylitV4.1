# ===================================================================
# == .dockerignore ULTRA-SÉCURISÉ - ANALYLIT V4.2 ATN ==
# ===================================================================
#
# Approche par "liste blanche" : tout ignorer, puis ré-inclure le strict nécessaire.
# Optimisé pour la thèse Alliance Thérapeutique Numérique

# 1. Ignorer absolument tout par défaut
**

# 2. Ré-inclure les fichiers et dossiers essentiels pour le build
!api/
!backend/
!database/
!docker/
!migrations/
!scripts/
!utils/
!web/
!alembic.ini
!docker-compose.yml
!requirements.txt
!setup.py

# ===================================================================
# == DONNÉES DE RECHERCHE ATN (INCLUSES DANS L'IMAGE) ==
# ===================================================================

source/                    # ❌ Monté via volume
zotero-storage/           # ❌ Monté via volume  
output/                   # ❌ Monté via volume
data/                     # ❌ Monté via volume

!templates/                 # ✅ Templates d'export thèse
!grille-ATN.json            # ✅ Grille extraction ATN

# ===================================================================
# == EXCLUSIONS DE SÉCURITÉ CRITIQUES ==
# ===================================================================
.env
.env.*
!.env.example
*.key
*.pem
*.p12
*.crt
*.csr

# ===================================================================
# == EXCLUSIONS DÉVELOPPEMENT LOURDES ==
# ===================================================================
node_modules/
venv/
__pycache__/
*.pyc
.pytest_cache/
.coverage
.git/
.gitignore
*.log
*.tmp

# ===================================================================
# == EXCLUSIONS FICHIERS VOLUMINEUX NON-ESSENTIELS ==
# ===================================================================
*.rar
*.7z
*.zip
*.tar.gz
output/logs/               # Logs de monitoring (persistants via volumes)
downloads/                 # Téléchargements temporaires
cache/                     # Cache applicatif

# ===================================================================
# == SCRIPTS DE TEST ATN (INCLUS DANS L'IMAGE) ==
# ===================================================================
!atn_workflow_GLORY.py        # ✅ AJOUT : Script de test
!atn_*.py          